import { PrismaClient } from "@prisma/client"; const prisma = new PrismaClient(); async function main() { console.log("Wiping..."); await prisma.issue.deleteMany(); await prisma.contribution.deleteMany(); await prisma.teamMember.deleteMany(); await prisma.repository.deleteMany(); await prisma.team.deleteMany(); await prisma.contributor.deleteMany(); await prisma.organization.deleteMany(); await prisma.systemState.upsert({ where: { id: 1 }, update: { syncLookbackDays: 90 }, create: { id: 1, syncLookbackDays: 90 } }); console.log("Done."); } main().finally(() => prisma.$disconnect());
